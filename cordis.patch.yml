# dsh-promptkit bundle for DeepSeek Harness (dsh).
#
# PromptKit 是纯浏览器端插件：方法工坊（PromptStudio）与快捷助手（QuickEnhancer）
# 根包同时提供 Cordis node half 和 ./client 浏览器导出。
# DSH 0.1.2+ 不扫描带斜杠的子路径 Loader 行，故不能以 dsh-promptkit/ui 挂载。
- insert:
    - id: dsh-promptkit
      name: dsh-promptkit
